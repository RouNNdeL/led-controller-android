<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorPrimaryDark">@color/color_primary_dark</item>
        <item name="colorAccent">@color/color_accent</item>
        <item name="android:textColorPrimary">@color/color_text_black_primary</item>
        <item name="android:textColorPrimaryInverse">@color/color_text_white_primary</item>
        <item name="android:textColorSecondary">@color/color_text_black_secondary</item>
        <item name="android:textColorSecondaryInverse">@color/color_text_white_secondary</item>
        <item name="colorControlNormal">?android:textColorSecondaryInverse</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="TextAppearance" parent="TextAppearance.AppCompat" />

</resources>
